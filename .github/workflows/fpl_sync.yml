Name: TW MM Tournament Sync Robot

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch: # လိုအပ်ရင် Manual နှိပ်လို့ရအောင် ထားထားပေးပါတယ်

jobs:
  fpl_sync_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Set up Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests firebase-admin

      # ၁။ ပွဲစဉ်ဇယားစစ်ဆေးပြီး အလိုအလျောက်ထုတ်ပေးခြင်း
      - name: Auto Run Fixture Generator
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: python scripts/fixture.py

      # ၂။ အမှတ်များ Sync လုပ်ခြင်း
      - name: Run FPL Rankings Sync
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: python scripts/sync_fpl.py

      # ၃။ Scout Data Sync လုပ်ခြင်း
      - name: Run Scout Firebase Sync
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: python scripts/sync_scouts.py
